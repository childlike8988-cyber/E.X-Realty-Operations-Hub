'use client'; import {useState} from 'react'; import type {Role} from '@/types/auth';
export function RolePreview(){const [role,setRole]=useState<Role>('ADMIN');return <div className="glass rounded-3xl p-6"><div className="text-sm text-slate-400">目前角色／部門</div><div className="mt-2 text-2xl font-semibold">{role}</div><p className="mt-2 text-sm text-slate-400">開發模式角色預覽，不是正式登入系統。</p><select value={role} onChange={e=>setRole(e.target.value as Role)} className="mt-5 w-full rounded-xl border border-slate-600 bg-slate-900 px-3 py-3 text-sm"><option value="ADMIN">行政示範部門</option><option value="SALES">業務示範部門</option><option value="MEDIA">影音示範部門</option><option value="MANAGER">主管示範部門</option></select></div>}
