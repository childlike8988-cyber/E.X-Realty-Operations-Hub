import Link from 'next/link';
import { ArrowUpRight, FileOutput } from 'lucide-react';
import type { CreativeProject } from '@/features/creative-studio/types';

export function ProjectCard({ project, propertyName, templateName }: { project: CreativeProject; propertyName: string; templateName: string }) {
  const time = project.updatedAt ? new Intl.DateTimeFormat('zh-TW', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(project.updatedAt)) : '尚未儲存';
  return <article className="rounded-2xl border border-slate-700 bg-slate-900/35 p-5"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><p className="text-xs tracking-[.14em] text-blue-200">{project.status}</p><h2 className="mt-2 truncate text-lg font-semibold">{project.name}</h2></div><span className={`inline-flex shrink-0 items-center gap-1 rounded-full border px-2 py-1 text-xs ${project.status === 'EXPORTED' ? 'border-emerald-300/30 bg-emerald-300/10 text-emerald-100' : 'border-slate-600 text-slate-300'}`}><FileOutput size={12}/>{project.status === 'EXPORTED' ? '已匯出' : '未匯出'}</span></div><dl className="mt-5 space-y-2 text-sm"><div className="flex justify-between gap-4"><dt className="text-slate-500">案件</dt><dd className="text-right text-slate-200">{propertyName}</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">模板</dt><dd className="text-right text-slate-200">{templateName}</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">最後修改</dt><dd className="text-right text-slate-400">{time}</dd></div></dl><Link href={`/tools/creative-studio/projects/${project.id}`} className="mt-5 inline-flex min-h-10 items-center gap-2 rounded-lg border border-blue-300/30 px-3 py-2 text-sm text-blue-100">查看詳情<ArrowUpRight size={15}/></Link></article>;
}
