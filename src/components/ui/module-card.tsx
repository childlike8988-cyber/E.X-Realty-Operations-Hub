import Link from 'next/link'; import {ArrowUpRight} from 'lucide-react';
export function ModuleCard({title,subtitle,description,route,status='planned',eyebrow}:{title:string;subtitle?:string;description:string;route:string;status?:string;eyebrow?:string}){return <Link href={route} className="glass module-card group flex min-h-36 flex-col justify-between rounded-2xl p-5 transition hover:-translate-y-0.5 hover:border-blue-300/40"><div><div className="mb-3 flex items-center justify-between"><span className="module-card-eyebrow text-xs uppercase tracking-[.16em]">{eyebrow??'模組'}</span><span className="module-card-status rounded-full border px-2 py-1 text-[10px]">{status==='planned'?'開發中':status}</span></div><h3 className="text-lg font-semibold">{title}</h3>{subtitle && <p className="module-card-subtitle">{subtitle}</p>}<p className="module-card-description mt-2 text-sm leading-6">{description}</p></div><div className="module-card-arrow mt-4 flex justify-end opacity-70 group-hover:opacity-100"><ArrowUpRight size={18}/></div></Link>}
