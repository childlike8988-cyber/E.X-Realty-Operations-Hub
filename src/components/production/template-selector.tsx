import type {Template} from '@/features/template-engine/types';
export function TemplateSelector({templates,selected,onSelect}:{templates:Template[];selected:string;onSelect:(id:string)=>void}){return <div className="space-y-2">{templates.map(t=><button key={t.id} onClick={()=>onSelect(t.id)} className={`w-full rounded-xl border p-3 text-left transition ${selected===t.id?'border-amber-300/60 bg-amber-300/10':'border-slate-700 bg-slate-900/40 hover:border-slate-500'}`}><div className="text-sm font-medium">{t.name}</div><div className="mt-1 text-xs text-slate-500">{t.category} · 固定模板</div></button>)}</div>}
