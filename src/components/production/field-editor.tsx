import type {TemplateField} from '@/features/template-engine/types'; import {ImageUploader} from './image-uploader';
export function FieldEditor({fields,data,onChange}:{fields:TemplateField[];data:Record<string,string>;onChange:(id:string,value:string)=>void}){return <div className="space-y-3">{fields.map(f=>f.type==='image'?<ImageUploader key={f.id} label={f.label} value={data[f.id]} onChange={v=>onChange(f.id,v)}/>:<label key={f.id} className="block text-sm text-slate-300">{f.label}{f.type==='textarea'?<textarea value={data[f.id]||''} onChange={e=>onChange(f.id,e.target.value)} rows={3} className="mt-1 w-full resize-y rounded-lg border border-slate-700 bg-slate-950/70 px-3 py-2 text-sm"/>:<input type={f.type==='date'?'date':'text'} value={data[f.id]||''} onChange={e=>onChange(f.id,e.target.value)} className="mt-1 w-full rounded-lg border border-slate-700 bg-slate-950/70 px-3 py-2 text-sm"/>}</label>)}</div>}
