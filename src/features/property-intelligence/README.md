# Property Intelligence Layer

This Mock-only layer composes a single `PropertyProfile` with existing Real Price transaction data and Location Intelligence data. It does not change Prisma, invoke AI services, or modify Proposal Studio.

`PropertyProposalContext` is the future handoff contract for Proposal Studio: Property Analysis → Proposal Studio. All current scores and insights are deterministic demonstrations and are not formal valuation advice.
