'use client'; import {useMemo,useState} from 'react'; import {PageHeader} from '@/components/ui/page-header';
export default function Mortgage(){const [price,setPrice]=useState(1200),[down,setDown]=useState(300),[rate,setRate]=useState(2.2),[years,setYears]=useState(30);const monthly=useMemo(()=>{const p=Math.max(price-down)*10000,r=rate/100/12,n=years*12;return r?Math.round(p*r*Math.pow(1+r,n)/(Math.pow(1+r,n)-1)):Math.round(p/n)},[price,down,rate,years]);return <div className="mx-auto max-w-4xl"><PageHeader title="房貸試算" description="使用示範數字快速估算每月本息攤還金額。"/><div className="grid gap-5 md:grid-cols-2"><div className="glass rounded-2xl p-5 space-y-4">{[['房屋總價（萬元）',price,setPrice],['頭期款（萬元）',down,setDown],['年利率（%）',rate,setRate],['貸款年限（年）',years,setYears]].map(([label,value,setter])=><label key={label as string} className="block text-sm text-slate-300">{label as string}<input type="number" value={value as number} onChange={e=>(setter as (n:number)=>void)(Number(e.target.value))} className="mt-2 w-full rounded-xl border border-slate-600 bg-slate-900 px-3 py-3"/></label>)}</div><div className="glass rounded-2xl p-6"><p className="text-sm text-slate-400">每月試算金額</p><p className="mt-3 text-4xl font-semibold text-blue-200">NT$ {monthly.toLocaleString()}</p><p className="mt-6 text-xs leading-6 text-amber-100/70">試算結果僅供參考，實際條件依金融機構核定為準。</p></div></div></div>}
