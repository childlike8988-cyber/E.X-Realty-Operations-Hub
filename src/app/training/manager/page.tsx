import { TrainingManagerExperience } from '@/components/training/training-manager-experience';

export const metadata = {
  title: 'Training Manager Preview | E.X Realty',
  robots: { index: false, follow: false },
};

export default function TrainingManagerPage() {
  return <TrainingManagerExperience />;
}
